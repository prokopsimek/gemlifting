language: ruby
cache: bundler

sevices:
  - postgresql
  - elasticsearch

before_script:
  - sleep 10
  
before_install:
  - createdb gemlifting_test
  - cp config/database.travis.yml config/database.yml

rvm:
  - 2.3.1
